Some mentions about the code:
-if you want to run it on your PC, make sure to change the PATH wherever is performed reading or writing from/in a file
-there are some commented lines with the instruction system("CLS"), make sure to uncomment them and comment the lines that print
multiple '\n's in order to run the app from the executable.
